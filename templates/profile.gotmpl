{{ define "profile" }}
		{{ template "head" . }}
		<title>Profile | Scamalytics</title>
	</head>
	<body>
		{{ template "header" . }}
		<main>
			<h1>{{ .Name }}</h1>
			<p>Joined {{ .Joined | humanizeTime }}</h1>
		</main>
		{{ template "footer" . }}
{{ end }}
